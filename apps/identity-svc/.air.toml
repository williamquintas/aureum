root = "."
tmp_dir = "tmp"

[build]
  bin = "tmp/server"
  cmd = "go build -o tmp/server ./cmd/server"
  delay = 1000
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_dir = ["tmp", "bin", "vendor", ".git"]

[log]
  time = true

[color]
  build = "yellow"
  running = "green"
