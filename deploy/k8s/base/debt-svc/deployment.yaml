apiVersion: apps/v1
kind: Deployment
metadata:
  name: debt-svc
  namespace: default
  labels:
    app: debt-svc
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxUnavailable: 0
      maxSurge: 1
  selector:
    matchLabels:
      app: debt-svc
  template:
    metadata:
      labels:
        app: debt-svc
    spec:
      containers:
        - name: debt-svc
          image: aureum/debt-svc:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50057
              name: grpc
              protocol: TCP
            - containerPort: 9098
              name: metrics
              protocol: TCP
          env:
            - name: GRPC_PORT
              value: "50057"
            - name: METRICS_PORT
              value: "9098"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: debt-db
                  key: write-dsn
            - name: REDIS_URL
              value: "redis:6379"
            - name: KAFKA_BROKERS
              value: "redpanda:9092"
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: debt-svc
                  key: jwt-secret
          livenessProbe:
            httpGet:
              path: /health
              port: 9098
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 9098
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
